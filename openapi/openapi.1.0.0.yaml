openapi: 3.0.0
info:
  title: Project API
  version: 1.0.0
tags: []
paths:
  /health:
    get:
      operationId: health
      parameters: []
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthCheck'
components:
  schemas:
    HealthCheck:
      type: object
      required:
        - status
      properties:
        status:
          type: string
    Versions:
      type: string
      enum:
        - 1.0.0
